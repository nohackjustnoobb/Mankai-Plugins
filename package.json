{
  "name": "mankai-plugin",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "build": "bun run build.ts"
  },
  "dependencies": {
    "js-md5": "^0.8.3"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "@types/node": "^26.5.0",
    "esbuild": "^0.28.2"
  }
}
